<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools" tools:context=".ShoppingList">
    <item
        android:title="Sort"
        android:id="@+id/sort_root"
        app:showAsAction="ifRoom|withText">
        <menu
            android:id="@+id/sort">
            <item android:id="@+id/sort_time"
                android:title="Sort By Time Added"/>
            <item android:id="@+id/sort_name"
                android:title="Sort By Name"/>
            <item android:id="@+id/sort_owner"
                android:title="Sort By Owner"/>
        </menu>
    </item>
    <item
        android:title="Delete"
        android:id="@+id/delete"
        app:showAsAction="ifRoom"
        android:icon="@mipmap/trash"/>

    <item
        android:title="Settings"
        android:id="@+id/settings"
        android:icon="@mipmap/settings"
        app:showAsAction="ifRoom">
        <menu>
            <item
                android:title="Disable Autocomplete"
                android:id="@+id/autocomplete"
                android:checkable="true"/>
            <item
                android:title="Clear Autocomplete Memory"
                android:id="@+id/clearAutocomplete"/>
        </menu>
    </item>
</menu>
